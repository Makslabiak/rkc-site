.DS_Store
Thumbs.db

# Local development artifacts
node_modules/
.env
.env.*
!.env.example

# OS/editor files
.idea/
.vscode/

# Локальный бэкап исходников перед пережатием (оригиналы есть в git history)
assets/images/figma/home-clean-original/

# Локальный бэкап крупных исходников перед пережатием
assets/images/_backup-original/

# Тяжёлые файлы держим рядом с проектом, а не в git: вместе они дают 117 МБ,
# а в истории этот вес остаётся навсегда. Ролик нужен сайту — он лежит на диске
# и выкладывается на сервер отдельно, мимо репозитория. Копия всех трёх
# каталогов: /Users/maksim/Figma-adaptive-heavy-files/
assets/videos/
assets/images/_original/
assets/images/about/raw/
