.DS_Store
Thumbs.db

# Local development artifacts
node_modules/
.env
.env.*
!.env.example

# OS/editor files
.idea/
.vscode/

# Сюда tools/to-webp.py складывает оригинал перед пережатием. Архив временный:
# долго хранить исходники в проекте не нужно, их место — Figma-adaptive-heavy-files/
assets/images/_backup-original/

# Ролик в git не хранится: 67 МБ, и в истории такой вес остаётся навсегда.
# Сайту он нужен — лежит на диске и выкладывается на сервер отдельно, мимо
# репозитория. Копия: /Users/maksim/Figma-adaptive-heavy-files/
assets/videos/

# Настройки почтовых приёмников: адреса почты, в репозитории им не место.
site-mail-config.php
