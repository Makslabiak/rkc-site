<?php
/**
 * Образец настроек для почтовых приёмников сайта: cookie-consent.php
 * (согласие на cookie) и contact-form.php (заявки с формы обратной связи).
 *
 * Что сделать на сервере:
 *  1. Скопировать этот файл на уровень выше папки сайта, рядом с ней, и
 *     назвать site-mail-config.php. Внутрь папки сайта класть нельзя:
 *     оттуда его смогут скачать.
 *  2. Вписать настоящие адреса.
 *
 * Почему адреса не лежат в самих скриптах: репозиторий открытый, и всё, что
 * в нём есть, видно любому. Здесь остаются только заглушки.
 *
 * Пока файла нет, приёмники работают на запасных значениях: письма идут на
 * общую почту компании, ту, что напечатана в подвале сайта.
 */

return [
    /* Кому идут письма о согласии на cookie. Несколько адресов — через запятую. */
    'cookie_to' => 'кому@пример.ru',

    /* Кому идут заявки с формы обратной связи. */
    'form_to' => 'кому@пример.ru',

    /* Адреса на время проверки. Пока они заполнены, письма идут туда, а не
       заказчику, и в теме появляется слово «проверка». Согласия при этом не
       склеиваются по одному в сутки, иначе перепроверить нельзя.
       После проверки оставить пустые кавычки. */
    'cookie_test_to' => '',
    'form_test_to' => '',

    /* От кого уходят письма. Домен обязан совпадать с доменом сайта, иначе
       почта получателя сочтёт письмо подделкой и положит в спам. */
    'from' => 'no-reply@пример.ru',

    /* Включить, только если сайт стоит за прокси или CDN: тогда адрес
       посетителя берётся из заголовка X-Forwarded-For. На прямом хостинге
       оставить false, иначе заголовок подделает кто угодно. */
    'trust_proxy' => false,
];
