<?php
/**
 * Образец настроек для cookie-consent.php.
 *
 * Что сделать на сервере:
 *  1. Скопировать этот файл на уровень выше папки сайта, рядом с ней, и
 *     назвать cookie-consent-config.php. Внутрь папки сайта класть нельзя:
 *     оттуда его смогут скачать.
 *  2. Вписать настоящие адреса.
 *
 * Почему адреса не лежат в самом скрипте: репозиторий открытый, и всё, что
 * в нём есть, видно любому. Здесь остаются только заглушки.
 *
 * Пока файла нет, скрипт работает с запасными значениями: письма идут на
 * общую почту компании, ту, что напечатана в подвале сайта.
 */

return [
    /* Кому идут письма о согласии. Несколько адресов — через запятую. */
    'to' => 'nazarova@пример.ru',

    /* Адрес на время проверки. Пока он заполнен, письма идут на него, а не
       заказчику, и в теме появляется слово «проверка». Повторные нажатия с
       одного браузера при этом не глушатся, иначе перепроверить нельзя.
       После проверки оставить пустые кавычки. */
    'test_to' => '',

    /* От кого. Домен обязан совпадать с доменом сайта, иначе почта
       получателя сочтёт письмо подделкой и отправит в спам. */
    'from' => 'no-reply@пример.ru',
];
